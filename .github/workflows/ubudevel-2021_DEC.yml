on:
  [push]
name: ubudevel - GNUstep 2021_DEC
jobs:
  testbuild:
    name: Building GNUstep
    runs-on: ubuntu-latest
    steps:
      - name: Check out the build script repository
        uses: actions/checkout@master
      - name: Execute test script
        run: |
          docker build -t gnustep-build-ci-ubudevel-2021_DEC ci-tests/ubudevel-2021_DEC/.
          docker run gnustep-build-ci-ubudevel-2021_DEC
